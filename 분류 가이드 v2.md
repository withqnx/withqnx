# 후기 분류 가이드 v3

이 문서는 AI 분류기와 사람이 함께 보는 **단일 기준점**.
`crawl.py`의 `CLASSIFY_SYSTEM` 상수와 이 문서는 항상 같은 내용을 유지한다.

> **v3에서 바뀐 것**: 카테고리 6개 하나로 뭉쳐 있던 것을 **대상·감정·의도 3개 축**으로 분리했다.
> 기존 6개 카테고리는 3축에서 **자동으로 계산되는 파생값**으로 계속 살아 있다(대시보드 호환).

---

## 왜 축을 나눴나

v2의 카테고리 6개는 성격이 다른 세 가지를 한 칸에 담고 있었다.

| v2 카테고리 | 실제로는 |
|---|---|
| 긍정 / 부정 / 중립 | **감정** |
| 배송관련불편 | **대상**(배송) x **감정**(부정) 이 붙어 있음 |
| 양도/거래, 교환/반품 | **의도(행동)** |

그래서 이런 것들이 표현되지 않았다.

- **"포장이 꼼꼼했다"** -> 배송 칭찬을 담을 칸이 없다. 카테고리 이름 자체가 "배송관련**불편**"이라서.
- **"상담 답변이 늦다"** -> 대상 축이 없어 상품 부정에 섞여 상품 품질 지표를 오염시킨다.
- **"사이즈가 안 맞아 교환해주세요"** -> 행동과 사유를 억지로 두 세그먼트로 쪼개야 했다(356건).
- **"케이스 언제 받나요?"** -> 담을 곳이 없어 중립/단순수령으로 들어갔다.

---

## 축 1. 대상 (aspect) - 무엇에 대한 말인가

**누가 고쳐야 하는 문제인지**가 갈린다.

| 값 | 정의 | 예 |
|---|---|---|
| `상품` | 제품 자체 - 품질/소재/사이즈/디자인/사용감. **제품에 동봉·부착된 안내문도 여기** | "봉제선이 튀어나왔어요" |
| `배송·포장` | 배송 속도/지연/박스 상태/오배송/누락/포장 | "박스가 찌그러져 왔어요", "포장이 정성스러웠어요" |
| `응대·소통` | 상담/문자/공지/답변/연락, **사이트·게시판 기능, 반품·선주문 정책** | "문의했는데 답이 없네요", "사이트 손 좀 보세요" |

- 판단이 서지 않으면 `상품`.
- **`배송·포장`과 `응대·소통`에는 해시태그를 붙이지 않는다** (태그는 상품 속성 어휘다).

## 축 2. 감정 (sentiment) - 어떻게 느꼈나

| 값 | 정의 |
|---|---|
| `긍정` | 만족/칭찬/감동 |
| `부정` | 불만/실망/문제 제기 |
| `중립` | 평가가 없음. 단순 수령 알림, 사실 진술, 질문만 있는 경우 |

## 축 3. 의도 (intent) - 무엇을 하려 하나

**운영에서 할 일**이 갈린다.

| 값 | 정의 | 예 |
|---|---|---|
| `없음` | 평가만 함 | "잘 쓰고 있어요" |
| `교환·반품` | **회사에** 교환/환불/AS 요청 | "교환 가능할까요?" |
| `양도·거래` | **구매자끼리** 양도/판매/맞교환 | "58호로 교환 원하시는 분" |
| `문의` | 질문/확인 요청 - 재고, 사용법, 배송 시점, AS 가능 여부 | "리필 심은 따로 살 수 있나요?" |
| `재구매희망` | **이미 파는 제품**의 재판매 요청/추가 구매 의사 | "재판매 해주세요", "더 살걸" |
| `제품제안` | **없는 제품/사양/후속 시즌을 만들어 달라는 요구** (감정이 아니므로 sentiment는 중립) | "여름용도 만들어주세요", "무지 흰양말 출시해주세요" |

- `교환·반품`과 `양도·거래`의 갈림길은 **상대가 회사냐 다른 구매자냐**다.
- `문의`와 `교환·반품`이 겹칠 때: **요청이면 `교환·반품`**, 단순 확인이면 `문의`.
  ("AS 되나요?" -> `문의` / "AS 해주세요" -> `교환·반품`)
- `재구매희망`은 v2의 `#또사고싶다` 태그가 올라온 것이다. **태그 목록에서 빠졌다.**

---

## 해시태그 15개 - `대상=상품`일 때만

v2의 14개에서 `#또사고싶다`가 의도 축으로 옮겨가고(-1), `#첫인상`과 `#선물`이 추가돼(+2) 15개가 됐다.

| 태그 | 정의 | 붙임 | 안 붙임 |
|---|---|---|---|
| `#사이즈` | 표기 치수/규격 평가 | "M인데 L 같아요", "생각보다 작아요" | 옷의 핏감 |
| `#핏` | (의류) 실루엣/기장/어깨라인 | "오버핏이라 좋아요" | 단순 크기 문제 |
| `#색상` | 색의 호불호/기대 일치 | "색이 예뻐요", "사진과 색 달라요" | 단순 색 언급 |
| `#마감` | 봉제선/이음새/마감처리, **스크래치·기스·자국·얼룩·단차·냄새** | "봉제선이 튀어나와요", "테에 자국이 있어요" | 막연한 "퀄리티" |
| `#재질` | 원단/소재/촉감/감촉 | "촉감 부드러워요", "원단이 얇아요" | - |
| `#내구성` | 사용 후 변형·손상, **보풀·털빠짐**, **"스크래치가 잘 생긴다"처럼 반복 발생** | "한 번 입었는데 늘어남" | 1회성 표면 하자는 `#마감` |
| `#디자인` | **구체적 형태/실루엣/패턴/디테일**을 짚을 때만 | "라운드 넥 라인 예뻐요" | 근거 없는 "예쁘다"는 `#첫인상`으로 |
| `#퀄리티` | **만듦새에 대한 평가**일 때만 | "고급스러워요", "잘 만들었어요" | "만족합니다/좋아요"에는 안 붙임 |
| `#가격` | 비싸다/싸다/가성비 | "이 가격에 이 정도면" | - |
| `#구성` | 포켓/세트/부속품 | "안쪽 주머니가 많아요" | - |
| `#기획` | **상품 설명의 약속 인용/반박이 명시될 때만** | "OO하다더니", "딱 저 같은 사람한테" | 단순 "좋아요"는 안 붙임 |
| `#무게감` | 무거움/가벼움 | "생각보다 가벼워요" | - |
| `#사용성` | 조립/설명 난이도, 사용 편의, 그립감, **동봉 안내문이 헷갈린다는 지적** | "조립이 어려워요", "세탁 방법 안내가 혼란스럽다" | - |
| `#첫인상` | **근거를 대지 않은 호평·혹평 전반** | "예뻐요", "실물이 더 낫네요", "만족합니다", "기다린 보람이 있다" | 구체적 형태가 나오면 `#디자인` |
| `#선물` | 선물이거나 남을 위해 산 것 | "아버지 생신에", "언니 선물로 샀는데" | - |

### 태그 절대 규칙

1. **이 15개 밖의 태그를 만들지 않는다.** `#소재 및 재질` `#필기감` `#그립감` `#쿠션감` 같은 창작 금지. 가장 가까운 기존 태그로 보내고, 없으면 붙이지 않는다.
2. **`#배송` 금지.** 대상 축의 `배송·포장`이 그 역할을 한다.
3. **"예쁘다" 단독에는 `#디자인`을 붙이지 않는다.** 구체적 형태 언급이 있어야 한다.
4. **`#퀄리티`는 단독일 때만.** `#마감`이나 `#재질`로 쪼개지면 그쪽을 쓴다.
5. **순수 감정 표현에는 어떤 태그도 붙이지 않는다.** "만족합니다", "좋아요", "감사합니다",
   "잘 받았습니다" — 무엇이 좋은지 말하지 않았으면 정보가 없는 것이다.
   **태그 없는 긍정은 정상이다.**
6. **헷갈리면 안 붙인다.**

---

## 판정 규칙 — 헷갈릴 때 이대로

이 쇼핑몰은 **선주문 후생산**이라 배송이 수개월 걸리는 것이 정상이고 고객도 알고 산다.

1. **이 후기가 달린 상품에 대한 말만 분류한다.** 본문에 다른 제품 얘기가 섞여 있어도
   그것으로는 세그먼트를 만들지 않는다.
2. **기다림·수령 사실은 중립.** "드디어 왔다", "N개월 만에", "기다린 보람"은
   `{배송·포장, 중립, 없음}`. 약속 일정 위반·연락 두절·주문 순서 역전·파손·오배송처럼
   **실제 불편**이 있을 때만 `{배송·포장, 부정}`.
3. **부속품 미수령은 `{배송·포장, 부정}`.** 단 **본품은 받았고 부속품만 기다리는 상태에서
   "언제 오나요"를 묻는 것은 `{배송·포장, 중립, 문의}`** — 분할배송을 이해하고 쓴 말투다.
   "빨리 보내주세요", "몇 달째입니다", "공지도 없네요"처럼 독촉·질책이 섞이면 그때 부정.
4. **교환·반품 vs 문의**: 본인이 겪은 **하자·불일치를 서술했으면 의문형이어도 `교환·반품`**
   ("사이즈가 커서 교환 가능한가요?"). 하자 서술 없이 정책·재고·배송시점·사용법만 물으면 `문의`.
5. **교환·반품 vs 양도·거래**: 상대가 회사면 교환·반품, 다른 구매자면 양도·거래
   ("~하실 분", "연락주세요", 전화번호·가격 명시). **제목과 본문이 어긋나면 `교환·반품`.**
6. **재구매·추가구매 의사는 가장 강한 긍정.** 의도가 `재구매희망`이면 감정은 원칙적으로
   `긍정`. 표현이 담백하거나("다음엔 색안경도 사고 싶네요") 품절을 아쉬워해도
   ("다른 색도 사려는데 판매가 끝났네요") 제품에 만족했다는 뜻이다. 아쉬움의 대상은
   제품이 아니라 판매 종료다.
7. **의도가 `문의`일 때 감정은 「불만을 말했는가」로 정한다.**
   - 중립: 사용법·요령·사양·수령 시점만 묻는 경우
     ("심지는 어느 쪽으로 돌리나요", "케이스는 언제 오나요")
   - 부정: 하자·미수령·지연·응대 불만을 서술한 뒤 묻는 경우
     ("보풀이 심한데 어떻게 쓰나요", "배송완료인데 못 받았습니다")

   질문했다는 사실만으로 부정을 주지 않는다. **기준은 문장 끝이 아니라 불만 서술의 유무.**
8. **confidence 는 개별 축의 판정이 흔들릴 때만 낮춘다.** 세그먼트를 2개 이상으로 나눈 것
   자체는 감점 사유가 아니다. 각 축이 본문에 명시돼 있으면 여러 개로 나눴어도 0.9 이상.

---

## 세그먼트를 나누는 기준

**`(대상, 감정, 의도)` 조합이 다르면 나눈다. 같으면 하나로 합치고 태그를 모은다.**

| 후기 | 세그먼트 |
|---|---|
| "잘 받았습니다" | `{상품, 중립, 없음}` |
| "배송 늦었지만 물건은 좋아요" | `{배송·포장, 부정, 없음}` + `{상품, 긍정, [태그]}` |
| "사이즈 안 맞아 교환해주세요" | `{상품, 부정, 교환·반품, [#사이즈]}` - **한 개** |
| "사이즈 안 맞아 양도합니다" | `{상품, 부정, 양도·거래, [#사이즈]}` - **한 개** |
| "케이스 언제 받나요?" | `{배송·포장, 중립, 문의}` |
| "리필 심 따로 살 수 있나요? 펜이 아까워요" | `{상품, 부정, 문의, [#구성]}` |
| "포장이 꼼꼼했어요" | `{배송·포장, 긍정, 없음}` |
| "상담 답변이 늦네요" | `{응대·소통, 부정, 없음}` |
| "색 예쁘고 재질 좋은데 밴딩이 약해요" | `{상품, 긍정, 없음, [#색상,#재질]}` + `{상품, 부정, 없음, [#마감]}` |

**v2에서 억지로 쪼개던 "행동 + 사유" 356건이 이제 한 세그먼트로 표현된다.**

---

## 신뢰도 (confidence)

| 범위 | 의미 |
|---|---|
| 0.9 ~ 1.0 | 단서 명확, 단일 해석 |
| 0.8 ~ 0.9 | 명확하지만 약간의 여지 |
| 0.6 ~ 0.8 | 애매함 -> `needs_review: true` |
| 0.6 미만 | 매우 애매함 -> `needs_review: true` 필수 |

**철칙: 헷갈리면 confidence를 낮춘다. 자신감 과장 금지.**

> v2에서 이 철칙이 지켜지지 않았다. 3,643건 중 confidence 0.8 미만이 **단 1건**이었고,
> 그러면서 `reasoning`에 "애매하다"고 써놓고 confidence 0.9 이상인 게 **193건**이었다.
> 검토 큐가 사실상 죽어 있었다는 뜻이다.
>
> **그래서 v3에는 자동 검증이 붙는다** - `reasoning`에 "애매/헷갈림/불확실/모호" 같은 말이
> 들어 있으면 후처리가 confidence를 **강제로 0.7로 낮추고 검토 큐로 보낸다.**
> 확신이 있으면 reasoning에 그런 말을 쓰지 말고, 애매하면 confidence를 정직하게 낮춰라.

---

## 출력 JSON 스키마

```json
{
  "segments": [
    {
      "aspect": "상품",
      "sentiment": "부정",
      "intent": "교환·반품",
      "tags": ["#사이즈"],
      "summary": "평소 치수인데 크게 나와 교환 요청",
      "confidence": 0.9
    }
  ],
  "confidence": 0.9,
  "needs_review": false,
  "reasoning": "치수 불일치라는 사유와 교환 요청이 한 덩어리"
}
```

- 최상위 `confidence` = segments 중 **최솟값**
- `needs_review` = `confidence < 0.8`
- `summary`는 **반드시 채운다**(빈 문자열 금지). 사람이 후기를 안 열어보고도 판단할 수 있어야 한다.
- 후기 단위 태그 합집합은 후처리가 자동 생성한다

---

## 파생 카테고리 (대시보드 호환)

기존 6개 카테고리는 3축에서 **아래 순서대로** 계산된다. 사람이 입력하지 않는다.

| 순서 | 조건 | 파생 카테고리 |
|---|---|---|
| 1 | `intent = 양도·거래` | 양도/거래 |
| 2 | `intent = 교환·반품` | 교환/반품 |
| 3 | `intent = 문의` | **문의** (신규) |
| 4 | `aspect = 배송·포장` 이고 `sentiment = 부정` | 배송관련불편 |
| 5 | `sentiment = 긍정` | 긍정 |
| 6 | `sentiment = 부정` | 부정 |
| 7 | 그 외 | 중립/단순수령 |

`intent = 재구매희망`은 파생 카테고리를 만들지 않고 **감정 규칙(5/6/7)을 따른다**.
재구매 의사는 별도 지표로 따로 센다.

---

## v2 -> v3 변환 (기존 데이터 소급)

2,374건은 아래 표로 **기계 변환**된다. 나머지 1,269건은 새로 분류한다
(문의형/요약 소실/규격 외 값/혼합 놓침 의심/응대 언급).

| v2 카테고리 | v3 |
|---|---|
| 긍정 | `{상품, 긍정, 없음}` |
| 부정 | `{상품, 부정, 없음}` |
| 배송관련불편 | `{배송·포장, 부정, 없음}` |
| 양도/거래 | `{상품, 중립, 양도·거래}` |
| 교환/반품 | `{상품, 중립, 교환·반품}` |
| 중립/단순수령 | `{상품, 중립, 없음}` |
| 태그 `#또사고싶다` | 태그에서 제거하고 `intent = 재구매희망` |

---

## 변경 이력

- **v3.3 (2026-09-22)**: 정답지 100건(사람 판정) 대조 결과 **오류 7건 중 5건이 감정 축**에
  몰린 것을 확인하고 규칙 2개 신설. `재구매희망`은 감정을 긍정으로 고정(93건 소급 정정),
  `문의`의 감정은 불만 서술 유무로 판정(순수 문의 23건 소급 중립화). 부속품 미수령
  예외를 "수령 시점 문의"까지 명시. 대상·의도 축은 100건에서 오류 0건이라 손대지 않음.

- **v3.2 (2026-09-17)**: 검토 큐 450건 전수 분석 후 판정 규칙 6개 신설. `제품제안` 의도
  신설(태그 15개 유지), 기다림 서술을 중립으로 고정, 교환·반품/문의 경계를 "하자 서술
  유무"로 확정, 안내문은 상품·사이트는 응대로 분리, 표면 하자/냄새는 `#마감`·보풀은
  `#내구성`, `#첫인상`을 근거 없는 호평 전반으로 확대, 세그먼트 분해를 confidence
  감점 사유에서 제외.

- **v3.1 (2026-09-16)**: `#첫인상`·`#선물` 추가(13->15개). 근거 없는 "예쁘다"가 `#디자인`으로
  새던 것(#디자인 768건의 22%)과 "만족합니다"가 `#퀄리티`로 새던 것(#퀄리티 544건의 29%)을
  각각 제자리로 보낸다. 순수 감정 표현에는 태그를 붙이지 않는 규칙 명문화.
- **v3 (2026-09-16)**: 카테고리 6개 -> **대상/감정/의도 3축**. `문의` 의도 신설,
  `#또사고싶다` -> `재구매희망` 의도로 승격(태그 14->13개), 응대·소통 대상 신설,
  reasoning-confidence 자동 검증 도입. 기존 6카테고리는 파생값으로 유지.
- **v2 (2026-05-19)**: 8 카테고리 -> 6 카테고리, 태그 10개 -> 14개, mixed 제거, 중립 단독 규칙 추가
- **v1**: 초기 (긍정/제품관련불편/배송관련불편/교환요청/반품요청/부정/중립/양도)
